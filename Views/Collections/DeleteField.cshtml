@model CollectorHub.ViewModels.CollectionFieldViewModel

@{
    ViewData["Title"] = "Удаление поля";
}

<h1>Удаление поля</h1>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">Удаление поля "@Model.Name"</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-danger">
                    <p><strong>Внимание!</strong> Вы собираетесь удалить поле "@Model.Name" типа "@Model.FieldTypeName".</p>
                    <p>Это действие удалит все данные об этом поле в предметах данной коллекции.</p>
                    <p>Это действие нельзя будет отменить.</p>
                </div>

                <form asp-action="DeleteFieldConfirmed" method="post">
                    <input type="hidden" name="fieldId" value="@Model.FieldId" />
                    <input type="hidden" name="collectionId" value="@Model.CollectionId" />

                    <div class="d-flex justify-content-between mt-3">
                        <button type="submit" class="btn btn-danger">Удалить</button>
                        <button type="button" onclick="window.history.back()" class="btn btn-secondary">Отмена</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>