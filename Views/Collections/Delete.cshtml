@model CollectorHub.Models.Collection

@{
    ViewData["Title"] = "Удаление коллекции";
}

<h1>@ViewData["Title"]</h1>

<div class="alert alert-danger">
    <h3>Вы уверены, что хотите удалить эту коллекцию?</h3>
    <p>Это действие нельзя будет отменить. Все предметы и поля коллекции также будут удалены.</p>
</div>

<div asp-validation-summary="All" class="text-danger"></div>

<div class="card mb-4">
    <div class="card-header">
        Информация о коллекции
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Название</dt>
            <dd class="col-sm-9">@Model.name</dd>

            @if (!string.IsNullOrEmpty(Model.description))
            {
                <dt class="col-sm-3">Описание</dt>
                <dd class="col-sm-9">@Model.description</dd>
            }

            <dt class="col-sm-3">Видимость</dt>
            <dd class="col-sm-9">@Model.visibility.name</dd>

            @if (Model.template != null)
            {
                <dt class="col-sm-3">Шаблон</dt>
                <dd class="col-sm-9">@Model.template.name</dd>
            }

            @if (Model.parent != null)
            {
                <dt class="col-sm-3">Родительская коллекция</dt>
                <dd class="col-sm-9">@Model.parent.name</dd>
            }
        </dl>
    </div>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="collection_id" />
    <input type="submit" value="Удалить" class="btn btn-danger" />
    <button type="button" onclick="window.history.back()" class="btn btn-secondary">Отмена</button>
</form>